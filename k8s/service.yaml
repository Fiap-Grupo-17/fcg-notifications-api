apiVersion: v1
kind: Service
metadata:
  name: notifications-api
  namespace: fcg
  labels:
    app: notifications-api
spec:
  selector:
    app: notifications-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
